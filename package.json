{
  "name": "cookie-migrator",
  "version": "1.0.0",
  "description": "Cookies manager extension that can import and export cookies",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "chrome-types": "^0.1.181",
    "typescript": "^5.0.3",
    "vite": "^4.2.1"
  }
}
